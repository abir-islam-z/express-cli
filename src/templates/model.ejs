import { model, Schema } from 'mongoose';
import { I<%= m_name %> } from './<%= name %>.interface';

const <%= name %>Schema = new Schema<I<%= m_name %>>({
  name: String,
});

export const <%= m_name %>Model = model<I<%= m_name %>>('<%= name %>', <%= name %>Schema);
